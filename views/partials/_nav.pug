.custom-nav
	a.logo.pull-left Voht
	if currentUser
		a.nav-option.pull-right(href='/logout') Log Out
	else 		
		a.nav-option.pull-right(href="/login") Login
	if currentUser	
		a.nav-option.pull-right(href='/profile') Profile
	else
		a.nav-option.pull-right(href="/register") Register	
	a.nav-option.pull-right(href="/") Home